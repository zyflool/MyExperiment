<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myexperiment">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RxJavaTest.RxJavaTestActivity"/>
        <activity android:name=".CircleImageView.CircleImageViewActivity" />
        <activity android:name=".Notification.NotificationActivity" />
        <activity android:name=".GridLayout.GridLayoutActivity" />
        <activity android:name=".getCompoundDrawables.GetCompoundDrawablesActivity" />
        <activity android:name=".RecyclerView.RecyclerViewActivity" />
        <activity android:name=".DragFloatingActionButton.DragFloatingActionButtonActivity" />
        <activity android:name=".CardView.CardViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>